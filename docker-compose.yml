version: "3.9"

services:
  db:
    image: mysql:5.7
    volumes:
      - db_data:/var/lib/mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: azerty
      MYSQL_DATABASE: mydb
      MYSQL_USER: root123
      MYSQL_PASSWORD: azerty

volumes:
  db_data: {}